import { DatosPalabra.Model } from './datos-palabra.model';

describe('DatosPalabra.Model', () => {
  it('should create an instance', () => {
    expect(new DatosPalabra.Model()).toBeTruthy();
  });
});
